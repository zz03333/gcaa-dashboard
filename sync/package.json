{
  "name": "fb-dashboard-sync",
  "version": "1.0.0",
  "description": "Sync Google Sheets data to Firestore for FB Dashboard",
  "main": "sheets-to-firestore.js",
  "scripts": {
    "sync": "node sheets-to-firestore.js",
    "sync:watch": "node sheets-to-firestore.js --watch"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "googleapis": "^130.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
